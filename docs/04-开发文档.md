# Vue 3.6 企业级中台管理系统 - 开发文档

## 一、环境准备

### 1.1 开发环境要求

| 工具 | 版本要求 | 说明 |
|------|---------|------|
| Node.js | ≥ 20.19.0 或 ≥ 22.12.0 | Vite 8 强制要求，推荐使用LTS版本 |
| pnpm | ≥ 9.0.0 | 包管理器，比npm/yarn更快更省空间 |
| Git | ≥ 2.30 | 版本控制 |
| VS Code | 最新版 | 推荐IDE，配合以下插件 |

VS Code必装插件：
- Volar（Vue 3.6官方推荐，禁用Vetur）
- Oxlint（Rust实现的Linter，速度比ESLint快100倍）
- Prettier（代码格式化）
- TypeScript Vue Plugin（Volar）
- Tailwind CSS IntelliSense（Tailwind智能提示）

### 1.2 环境安装验证

```bash
node -v   # 确认版本 v20.19.0+
pnpm -v   # 确认版本 v9.0.0+
git --version
```

---

## 二、项目初始化步骤

### 2.1 创建项目（从零开始）

```bash
# 1. 创建项目目录
mkdir vue36-enterprise-admin && cd vue36-enterprise-admin

# 2. 初始化pnpm workspace Monorepo
pnpm init

# 3. 创建目录结构（DDD分层）
mkdir -p apps/admin src/{presentation,application,domain,infrastructure,stores,hooks,utils,styles}
mkdir -p packages/{components,utils,hooks,types,styles}

# 4. 初始化主应用
cd apps/admin
pnpm create vite@latest . -- --template vue-ts
```

### 2.2 安装核心依赖

```bash
# 核心框架
pnpm add vue@^3.6 vue-router@^5.1 pinia@^3.0

# UI组件库 + 样式
pnpm add ant-design-vue@^4.2 @ant-design/icons-vue
pnpm add -D tailwindcss @tailwindcss/vite less

# 工具库
pnpm add axios@^1.7 @vueuse/core@^12 dayjs
pnpm add nprogress js-cookie crypto-js

# 开发依赖 Vite 8.1
pnpm add -D vite@^8.1 @vitejs/plugin-vue typescript@^7.0 vue-tsc
pnpm add -D oxlint prettier
pnpm add -D @oxlint/vite-plugin-vue
pnpm add -D unplugin-auto-import unplugin-vue-components
pnpm add -D @types/node @types/js-cookie @types/crypto-js
pnpm add -D vitest @vue/test-utils @vitest/ui jsdom
pnpm add -D web-vitals

# Monorepo构建工具
pnpm add -D turbo -w
```

### 2.3 package.json 脚本配置

```json
{
  "name": "vue36-admin",
  "scripts": {
    "dev": "vite",
    "dev:bundled": "VITE_BUNDLED_DEV=true vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "oxlint . --fix --deny-warnings",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "vue-tsc --noEmit"
  }
}
```

---

## 三、核心配置文件

### 3.1 vite.config.ts 完整配置

```ts
import { defineConfig, loadEnv } from 'vite'
import vue from '@vitejs/plugin-vue'
import vueJsx from '@vitejs/plugin-vue-jsx'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { AntDesignVueResolver } from 'unplugin-vue-components/resolvers'
import { fileURLToPath } from 'node:url'

export default defineConfig(({ mode }) => {
  const env = loadEnv(mode, process.cwd())
  return {
    plugins: [
      vue({
        // Vue 3.6 Vapor Mode 启用
        vapor: true,
      }),
      vueJsx(),
      // 自动导入API
      AutoImport({
        imports: ['vue', 'vue-router', 'pinia', '@vueuse/core'],
        dts: 'src/auto-imports.d.ts',
        dirs: ['src/hooks', 'src/stores', 'src/utils'],
      }),
      // 自动注册组件
      Components({
        resolvers: [AntDesignVueResolver({ importStyle: 'less' })],
        dts: 'src/components.d.ts',
        dirs: ['src/presentation/components'],
      }),
    ],
    // Vite 8.1 打包开发模式（大型项目提速15倍）
    server: {
      bundled: process.env.VITE_BUNDLED_DEV === 'true',
      port: 3000,
      host: '0.0.0.0',
      proxy: {
        '/api': {
          target: env.VITE_API_BASE_URL || 'http://localhost:8080',
          changeOrigin: true,
          rewrite: (path) => path.replace(/^\/api/, ''),
        },
      },
    },
    resolve: {
      alias: {
        '@': fileURLToPath(new URL('./src', import.meta.url)),
        '@presentation': fileURLToPath(new URL('./src/presentation', import.meta.url)),
        '@application': fileURLToPath(new URL('./src/application', import.meta.url)),
        '@domain': fileURLToPath(new URL('./src/domain', import.meta.url)),
        '@infra': fileURLToPath(new URL('./src/infrastructure', import.meta.url)),
      },
    },
    css: {
      preprocessorOptions: {
        less: {
          javascriptEnabled: true,
          modifyVars: {
            // Ant Design Vue 主题定制
            '@primary-color': '#1890ff',
          },
        },
      },
    },
    build: {
      target: 'es2022',
      sourcemap: false,
      rollupOptions: {
        output: {
          manualChunks: {
            vue: ['vue', 'vue-router', 'pinia'],
            antd: ['ant-design-vue', '@ant-design/icons-vue'],
            utils: ['axios', '@vueuse/core', 'dayjs'],
          },
        },
      },
    },
  }
})
```

### 3.2 tsconfig.json 配置

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "jsx": "preserve",
    "sourceMap": true,
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "paths": {
      "@/*": ["./src/*"],
      "@presentation/*": ["./src/presentation/*"],
      "@application/*": ["./src/application/*"],
      "@domain/*": ["./src/domain/*"],
      "@infra/*": ["./src/infrastructure/*"]
    },
    "types": ["vite/client", "node"]
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.tsx", "src/**/*.vue"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

### 3.3 Oxlint 配置（Rust 高性能 Linter）

Oxlint 是用 Rust 编写的 JavaScript/TypeScript/Vue Linter，比 ESLint 快 **50-100 倍**，大型项目 lint 从分钟级降到秒级，完美适配 Vite 8.1 的 Rust 技术栈。零配置兼容 ESLint 规则，配置文件体积更小。

#### 3.3.1 oxlint.config.json 完整配置

```json
{
  "plugins": ["vue", "typescript", "import", "unicorn"],
  "files": ["src/**/*.{js,ts,vue,tsx}"],
  "ignores": ["node_modules", "dist", "*.config.*"],
  "rules": {
    // Vue 规则
    "vue/multi-word-component-names": "off",
    "vue/no-unused-vars": "error",
    "vue/require-default-prop": "off",
    "vue/html-self-closing": "warn",
    "vue/max-attributes-per-line": "off",
    "vue/singleline-html-element-content-newline": "off",

    // TypeScript 规则
    "typescript/no-explicit-any": "warn",
    "typescript/no-unused-vars": "error",
    "typescript/no-inferrable-types": "off",
    "typescript/consistent-type-imports": "error",

    // 通用规则
    "no-console": ["warn", { "allow": ["warn", "error"] }],
    "no-debugger": "warn",
    "no-unused-vars": "off",
    "eqeqeq": ["error", "smart"],
    "prefer-const": "error",
    "no-var": "error",

    // Import 规则
    "import/order": ["error", {
      "groups": ["builtin", "external", "internal", "parent", "sibling", "index"],
      "newlines-between": "always"
    }],

    // Unicorn 最佳实践
    "unicorn/filename-case": ["error", { "case": "kebabCase" }],
    "unicorn/prefer-includes": "error",
    "unicorn/no-null": "off"
  }
}
```

#### 3.3.2 Vite 8.1 插件集成（开发时实时检查）

```ts
// vite.config.ts 中新增
import oxlint from '@oxlint/vite-plugin-vue'

export default defineConfig({
  plugins: [
    vue({ vapor: true }),
    // Oxlint 开发时实时检查，有错误直接显示在 Vite overlay 上
    oxlint({
      // 开发环境只显示错误，警告输出到控制台
      failOnError: false,
      // 是否显示警告
      warnings: true,
    }),
    // ...其他插件
  ],
})
```

#### 3.3.3 Oxlint vs ESLint 性能对比

| 对比项 | ESLint（JS实现） | Oxlint（Rust实现） | 提升倍数 |
|-------|-----------------|-------------------|---------|
| 1000个文件lint时间 | 60-120秒 | 0.5-2秒 | 50-100x |
| 5000个文件lint时间 | 5-10分钟 | 3-8秒 | 60-120x |
| 内存占用 | 500MB-2GB | 50-200MB | 5-10x |
| 配置复杂度 | 高（大量插件依赖） | 低（内置主流规则） | - |
| 冷启动时间 | 3-5秒 | <50ms | 60-100x |
| Vue支持 | 需eslint-plugin-vue | 内置Vue插件 | - |

> 💡 **选型建议**：开发阶段使用 Vite Oxlint 插件做实时检查，CI/CD 阶段使用 `oxlint .` 做全量检查，开发体验和CI速度都大幅提升。

---

## 四、完整日志体系实现

### 4.1 日志封装核心代码（src/infrastructure/logger/index.ts）

```ts
type LogLevel = 'DEBUG' | 'INFO' | 'WARN' | 'ERROR' | 'FATAL'

interface LogEntry {
  timestamp: string
  level: LogLevel
  module: string
  message: string
  stack?: string
  userId?: string | number
  url?: string
  userAgent?: string
  duration?: number
  extra?: Record<string, any>
}

class Logger {
  private level: LogLevel = import.meta.env.DEV ? 'DEBUG' : 'ERROR'
  private cache: LogEntry[] = []
  private maxCache = 100

  private formatEntry(level: LogLevel, module: string, message: string, extra?: any): LogEntry {
    return {
      timestamp: new Date().toISOString(),
      level,
      module,
      message,
      stack: extra?.stack,
      url: window.location.href,
      userAgent: navigator.userAgent,
      extra,
    }
  }

  private output(entry: LogEntry) {
    // 1. 控制台美化输出
    const colors = {
      DEBUG: '#888',
      INFO: '#1890ff',
      WARN: '#faad14',
      ERROR: '#ff4d4f',
      FATAL: '#8b0000',
    }
    console.log(
      `%c[${entry.level}]%c[${entry.module}]%c ${entry.message}`,
      `color:${colors[entry.level]};font-weight:bold`,
      'color:#666',
      'color:#333',
      entry.extra || ''
    )

    // 2. 本地缓存最近100条
    this.cache.push(entry)
    if (this.cache.length > this.maxCache) {
      this.cache.shift()
    }

    // 3. 生产环境ERROR级别上报
    if (import.meta.env.PROD && ['ERROR', 'FATAL'].includes(entry.level)) {
      this.report(entry)
    }
  }

  private async report(entry: LogEntry) {
    // 上报到日志服务接口
    try {
      await fetch('/api/log/report', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(entry),
        keepalive: true,
      })
    }
    catch (e) {
      // 上报失败不影响主流程
    }
  }

  debug(module: string, message: string, extra?: any) {
    if (this.level === 'DEBUG')
      this.output(this.formatEntry('DEBUG', module, message, extra))
  }

  info(module: string, message: string, extra?: any) {
    this.output(this.formatEntry('INFO', module, message, extra))
  }

  warn(module: string, message: string, extra?: any) {
    this.output(this.formatEntry('WARN', module, message, extra))
  }

  error(module: string, message: string, error?: Error | any) {
    this.output(this.formatEntry('ERROR', module, message, {
      stack: error?.stack,
      ...error,
    }))
  }

  // 获取缓存日志（用于调试/上报）
  getCache() {
    return [...this.cache]
  }
}

export const logger = new Logger()
```

### 4.2 全局错误捕获配置

```ts
// src/main.ts
import { createApp } from 'vue'
import App from './App.vue'
import { logger } from '@infra/logger'

const app = createApp(App)

// 1. Vue组件渲染错误
app.config.errorHandler = (err, instance, info) => {
  logger.error('vue', `组件渲染错误: ${info}`, err)
}

// 2. 全局未捕获错误
window.addEventListener('error', (event) => {
  if (event.target && (event.target as any).tagName) {
    // 静态资源加载错误
    logger.error('resource', `资源加载失败: ${(event.target as any).src || (event.target as any).href}`, event)
  }
  else {
    logger.error('global', '全局运行时错误', event.error)
  }
}, true)

// 3. 未捕获的Promise错误
window.addEventListener('unhandledrejection', (event) => {
  logger.error('promise', '未捕获的Promise异常', event.reason)
})

// 4. Web Vitals 性能监控
import('web-vitals').then(({ onCLS, onFID, onLCP, onFCP }) => {
  const sendPerf = (name: string) => (metric: any) => {
    // 生产环境采样10%上报
    if (import.meta.env.PROD && Math.random() > 0.1)
      return
    logger.info('perf', `${name}: ${metric.value}ms`, metric)
  }
  onCLS(sendPerf('CLS'))
  onFID(sendPerf('FID'))
  onLCP(sendPerf('LCP'))
  onFCP(sendPerf('FCP'))
})
```

### 4.3 Axios接口日志拦截器

```ts
// src/infrastructure/http/index.ts
import axios from 'axios'
import { logger } from '@infra/logger'
import { message } from 'ant-design-vue'

const http = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || '/api',
  timeout: 30000,
})

// 请求拦截器
http.interceptors.request.use((config) => {
  config.metadata = { startTime: Date.now() }
  const token = localStorage.getItem('token')
  if (token)
    config.headers.Authorization = `Bearer ${token}`
  return config
})

// 响应拦截器
http.interceptors.response.use(
  (response) => {
    const duration = Date.now() - response.config.metadata.startTime
    // 慢接口告警 >3s
    if (duration > 3000) {
      logger.warn('api', `慢接口告警: ${response.config.url} 耗时${duration}ms`, {
        url: response.config.url,
        duration,
        params: response.config.params,
      })
    }
    else {
      logger.debug('api', `${response.config.method?.toUpperCase()} ${response.config.url} ${duration}ms`)
    }
    return response.data
  },
  (error) => {
    logger.error('api', `接口请求失败: ${error.config.url}`, error)
    message.error(error.message || '请求失败')
    return Promise.reject(error)
  },
)

export default http
```

---

## 五、TDD 单元测试配置

### 5.1 Vitest配置（vitest.config.ts）

```ts
import { defineConfig } from 'vitest/config'
import vue from '@vitejs/plugin-vue'
import { fileURLToPath } from 'node:url'

export default defineConfig({
  plugins: [vue({ vapor: true })],
  test: {
    environment: 'jsdom',
    globals: true,
    coverage: {
      provider: 'v8',
      reporter: ['text', 'html', 'json'],
      thresholds: {
        global: {
          statements: 80,
          branches: 80,
          functions: 80,
          lines: 80,
        },
        // 核心领域层要求100%覆盖
        'src/domain/**/*.ts': {
          statements: 100,
          branches: 100,
          functions: 100,
          lines: 100,
        },
      },
    },
  },
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
  },
})
```

### 5.2 单元测试示例（领域层用户实体测试）

```ts
// src/domain/user/__tests__/user.entity.spec.ts
import { describe, it, expect } from 'vitest'
import { UserEntity } from '../user.entity'

describe('UserEntity', () => {
  it('should create a valid user', () => {
    const user = UserEntity.create({
      username: 'admin',
      nickname: '管理员',
      status: 0,
    })
    expect(user.username).toBe('admin')
    expect(user.isActive()).toBe(true)
  })

  it('should validate password strength', () => {
    expect(() => UserEntity.validatePassword('123')).toThrow('密码长度至少6位')
    expect(() => UserEntity.validatePassword('abcdef')).toThrow('密码必须包含字母和数字')
    expect(UserEntity.validatePassword('abc123')).toBe(true)
  })
})
```

---

## 六、目录结构创建脚本

执行以下命令一键创建DDD分层完整目录：

```bash
#!/bin/bash
# scripts/init-structure.sh
set -e

BASE_DIR="src"

# DDD分层目录
mkdir -p $BASE_DIR/presentation/{components,layouts,views/{system/{user,role,menu,dept,dict,config,log,online,tenant},login,dashboard,error},directives,router,styles}
mkdir -p $BASE_DIR/application/{services,dto,commands,queries}
mkdir -p $BASE_DIR/domain/{shared,user,role,menu,dept,dict}
mkdir -p $BASE_DIR/infrastructure/{repositories,http,cache,logger,storage}
mkdir -p $BASE_DIR/{stores/modules,hooks,utils}

echo "✅ DDD目录结构创建完成"
```

---

## 七、开发规范

### 7.1 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 组件文件 | PascalCase | UserTable.vue |
| 页面文件 | kebab-case | user-list.vue |
| 组合式函数 | use前缀，camelCase | useUserTable.ts |
| 工具函数 | camelCase | formatDate.ts |
| 常量 | UPPER_SNAKE_CASE | MAX_PAGE_SIZE |
| CSS类名 | kebab-case | .user-table-wrapper |

### 7.2 Git提交规范

使用Conventional Commits规范：

```
feat: 新增功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
test: 测试相关
chore: 构建/工具相关
perf: 性能优化
```